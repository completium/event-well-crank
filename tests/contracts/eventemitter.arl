archetype eventemitter(well : address)

record event {
  ival : nat;
  sval : string;
}

entry emit (i : nat, s : string) {
  var e : event = { i; s };
  var p : bytes = pack(e);
  transfer 0tz to well call default<bytes>(p)
}